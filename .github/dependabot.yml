version: 2
updates:
  # Keep pinned GitHub Actions SHAs current
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"

  # Track AWS provider / module version updates
  - package-ecosystem: "terraform"
    directory: "/"
    schedule:
      interval: "weekly"
